# Example environment variables for RAG service
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o
FLASK_ENV=development
PORT=8000
RAG_TOP_K=5

# If you use a persistent Chroma DB set a path; otherwise defaults to in-memory
CHROMA_PERSIST_DIR=./.chromadb

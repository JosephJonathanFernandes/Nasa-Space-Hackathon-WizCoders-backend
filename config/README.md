# Config Directory

This folder contains configuration files and environment setup instructions for local development, staging, and production.

- Place environment-specific config files here (e.g., `dev.env`, `prod.env`)
- Do not store secrets or credentials in this folder
- See `.env.example` in the project root for required variables
